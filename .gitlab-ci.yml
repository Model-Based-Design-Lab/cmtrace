build:
    tags:
        - Ubuntu1804-64bit
    script:
        - sudo apt-get -y update
        - sudo apt-get -y install python3-pip libcairo2-dev
        - pwd
        - ls
        - cd  ./package/cmtrace/cmtrace/tests
        - python -m pytest . -v

    artifacts:
      expire_in: 14d
      when: always
      paths:
      - /builds/computational-modeling/cmtrace/package/cmtrace/cmtrace/test/output/
